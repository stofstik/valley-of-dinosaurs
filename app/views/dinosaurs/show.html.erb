<h1><%= @dinosaur.name %></h1>
<h2>age: <%= @dinosaur.age %></h2>
<%= image_tag @dinosaur.image_url %>

<%= link_to "[Edit]", edit_dinosaur_path( @dinosaur.id ) %>
<%= link_to "[Delete]", dinosaur_path( @dinosaur.id ), method: :delete, data: { confirm: "Are you sure?" } %>
